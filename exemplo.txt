programa TESTE;

var
    x, y, z : inteiro;
    ativo : booleano;

inicio
    x := 10 + 5 * 2;
    y := (x - 4) / 2;
    ativo := x >= y;

    escreva(x);
    escreva(100);
    escreva(x + y);
    escreva(ativo);

    se x >= y entao
        escreva(x);
    senao
        escreva(y);
    fimse;

    se ativo == verdadeiro entao
        escreva(100);
    fimse;

    enquanto x < 20 faca
        x := x + 1;
        escreva(x);
    fimenquanto;

    enquanto x < y faca
        x := x + 1;

        se x == 15 entao
            escreva(x);
        fimse;
    fimenquanto;

fim.
